---
phase: 16-playwright-e2e
plan: 02
type: execute
wave: 2
depends_on:
  - 16-01
files_modified:
  - e2e/tests/auth.smoke.spec.ts
autonomous: true
requirements:
  - E2E-02
must_haves:
  truths:
    - Register flow navigates to /shelf after successful account creation
    - Login with valid credentials navigates to /shelf
    - Login with invalid credentials stays on /login and shows "Invalid email or password."
    - Logout clears the booktracker_token from localStorage and navigates to /login
  artifacts:
    - e2e/tests/auth.smoke.spec.ts
  key_links:
    - auth.smoke.spec.ts overrides storageState to empty (unauthenticated) for all tests except logout which starts authenticated
    - The logout test uses the pre-saved user-a.json storageState — relies on auth.setup.ts from Plan 01
---

<objective>
Implement the auth smoke test spec covering all four auth flows required by E2E-02: register new user, login with valid credentials, login with invalid credentials, and logout with token clearance.

Purpose: Pins the auth entry points so a regression in LoginPage, RegisterPage, or ProfilePage sign-out is caught immediately.

Output: `e2e/tests/auth.smoke.spec.ts` — four tests, all passing green with `npx playwright test tests/auth.smoke.spec.ts --project=chromium`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/16-playwright-e2e/16-RESEARCH.md
@.planning/phases/16-playwright-e2e/16-UI-SPEC.md
@.planning/phases/16-playwright-e2e/16-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auth smoke spec — register, login valid, login invalid, logout (E2E-02)</name>
  <read_first>
    .planning/phases/16-playwright-e2e/16-UI-SPEC.md — Copywriting Contract table (exact strings for button names, error messages, headings) and E2E-02 Locator Contract table
    .planning/phases/16-playwright-e2e/16-RESEARCH.md — Auth Smoke Test code example (Pattern 1 locator strategy, auth.smoke.spec.ts skeleton)
    e2e/tests/auth.setup.ts — verify AUTH_FILE_A path used by the logout test
    e2e/playwright.config.ts — confirm baseURL is http://localhost:8080
  </read_first>
  <files>
    e2e/tests/auth.smoke.spec.ts
  </files>
  <action>
    Create `e2e/tests/auth.smoke.spec.ts`. All four tests use unauthenticated context except logout. Use ONLY these locator strategies: `getByLabel`, `getByRole`, `getByText`, `getByTestId` — never CSS selectors.

    Apply `test.use({ storageState: { cookies: [], origins: [] } })` at the file level so all four tests start without any session token. The logout test must load User A's storageState explicitly via `page.context().addInitScript` — actually: apply file-level unauthenticated, then for the logout test manually navigate to `/login`, log in via the form, then log out. This is cleaner than mixing storageState overrides.

    Test 1 — "register new user": `page.goto('/register')`, fill `getByLabel('Display Name')` with `E2E Reg User`, fill `getByLabel('Email')` with a unique address using `Date.now()` (e.g. `` `reg-${Date.now()}@example.com` ``), fill `getByLabel('Password')` with `securepassword`, click `getByRole('button', { name: 'Create Account' })`, assert `toHaveURL('/shelf')`, assert `getByRole('heading', { name: 'My Shelf' })` is visible.

    Test 2 — "login with valid credentials": `page.goto('/login')`, fill `getByLabel('Email')` with `e2e-user-a@example.com`, fill `getByLabel('Password')` with `password123`, click `getByRole('button', { name: 'Sign In' })`, assert `toHaveURL('/shelf')`.

    Test 3 — "login with invalid credentials shows error": `page.goto('/login')`, fill `getByLabel('Email')` with `nobody@nowhere.example.com`, fill `getByLabel('Password')` with `wrongpassword`, click `getByRole('button', { name: 'Sign In' })`, assert `getByText('Invalid email or password.')` is visible, assert `toHaveURL('/login')`.

    Test 4 — "logout clears token and redirects to login": Since the file-level storageState is unauthenticated, this test must first log in programmatically. Do so by: `page.goto('/login')`, fill + click Sign In with User A credentials, `expect(page).toHaveURL('/shelf')`, then navigate to `/profile`, click `getByTestId('logout-button')`, assert `toHaveURL('/login')`, then assert `page.evaluate(() => localStorage.getItem('booktracker_token'))` resolves to `null`.

    Important: `getByText('Invalid email or password.')` must match the exact copy from the UI-SPEC Copywriting Contract. `getByRole('button', { name: 'Sign In' })` and `getByRole('button', { name: 'Create Account' })` must match the exact button text in those pages. Do not use `getByRole('button', { name: 'Signing in…' })` — that is the pending state text.
  </action>
  <verify>
    <automated>
      cd /home/katlego/projects/booktracker/e2e && npx playwright test tests/auth.smoke.spec.ts --project=chromium
    </automated>
  </verify>
  <acceptance_criteria>
    - `e2e/tests/auth.smoke.spec.ts` exists
    - File-level `test.use({ storageState: { cookies: [], origins: [] } })` is present (all tests start unauthenticated)
    - `grep -c "getByTestId('logout-button')" e2e/tests/auth.smoke.spec.ts` equals 1
    - `grep -c "Invalid email or password" e2e/tests/auth.smoke.spec.ts` equals 1
    - `grep -c "localStorage.getItem('booktracker_token')" e2e/tests/auth.smoke.spec.ts` equals 1
    - `npx playwright test tests/auth.smoke.spec.ts --project=chromium` exits 0 with "4 passed"
    - No `page.waitForTimeout` calls in the file (use web-first assertions only)
    - No CSS selector strings (no `.class-name` or `div >` patterns)
  </acceptance_criteria>
  <done>auth.smoke.spec.ts passes 4 tests: register redirects to /shelf, valid login redirects to /shelf, invalid login shows "Invalid email or password." and stays on /login, logout clears booktracker_token and redirects to /login.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E test → /api/auth/login | Test sends real credentials to the running jar; invalid-credentials test verifies the 401 path |
| Test file → localStorage | Logout test reads localStorage via `page.evaluate` to assert token absence |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-05 | Tampering | auth.smoke.spec.ts using `Date.now()` in email for register test | low | accept | Dynamic email ensures no conflict with seeded users. Risk of test-data accumulation in E2E DB is acceptable — DB is ephemeral in CI and locally resettable. |
| T-16-06 | Information Disclosure | Test file hardcodes `password123` seed credential | low | accept | Same rationale as T-16-01 — fixed test credential, isolated DB, no production exposure. |
</threat_model>

<verification>
After task completes:

1. `ls e2e/tests/auth.smoke.spec.ts` — file exists
2. `cd e2e && npx playwright test tests/auth.smoke.spec.ts --project=chromium` — all 4 tests pass
3. Playwright HTML report shows 4 green tests under `auth.smoke.spec.ts`
</verification>

<success_criteria>
- `auth.smoke.spec.ts` exists with 4 tests covering E2E-02 scenarios
- All 4 tests pass: register → /shelf, valid login → /shelf, invalid login → error visible + stays on /login, logout → token null + /login
- No flaky waits (only web-first assertions used)
- No CSS selector locators
</success_criteria>

<output>
Create `.planning/phases/16-playwright-e2e/16-02-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

| Artifact | Path | Type |
|----------|------|------|
| Auth smoke spec | `e2e/tests/auth.smoke.spec.ts` | new file |
