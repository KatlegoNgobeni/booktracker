.planning/
.claude/

# Maven build output
target/

# IDE files
.idea/
*.iml
*.ipr
*.iws
.vscode/
.settings/
.classpath
.project
*.launch

# Local environment files — .env.example IS tracked; .env and variants are NOT
.env
.env.*
!.env.example

# Local docker-compose tweaks (e.g. exposing the db port for host tools)
docker-compose.override.yml

# OS cruft
.DS_Store
Thumbs.db

# Playwright E2E — auth state files, reports, and test result artifacts
e2e/playwright/.auth/
e2e/playwright-report/
e2e/test-results/
e2e/node_modules/
# e2e/package-lock.json — intentionally tracked for CI cache

# Frontend build output copied into resources/static for jar packaging
src/main/resources/static/
