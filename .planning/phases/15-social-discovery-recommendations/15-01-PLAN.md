---
phase: 15-social-discovery-recommendations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/social/FeedPage.test.tsx
  - frontend/src/hooks/__tests__/useSocial.test.ts
  - src/test/java/com/booktracker/social/SocialIntegrationTest.java
autonomous: true
requirements:
  - DISC-01
  - DISC-02
  - DISC-03
  - DISC-04

must_haves:
  truths:
    - Test files exist and fail with "not implemented" or import errors before any implementation
    - FeedPage.test.tsx covers DISC-01 (discovery sections render), DISC-02 (book card links), DISC-04 (activity feed section)
    - useSocial.test.ts covers DISC-03 (follow/unfollow invalidates profile query)
    - SocialIntegrationTest.java has a test method for GET /api/feed/friends-reading scoping
  artifacts:
    - frontend/src/pages/social/FeedPage.test.tsx
    - frontend/src/hooks/__tests__/useSocial.test.ts (extended)
    - src/test/java/com/booktracker/social/SocialIntegrationTest.java (extended)
  key_links:
    - FeedPage.test.tsx MSW handlers mock /api/feed and /api/feed/friends-reading
    - useSocial.test.ts imports QUERY_KEYS from queryKeys.ts to assert invalidation keys
    - SocialIntegrationTest seeds two users, ACCEPTED friend_request row, and a CURRENTLY_READING user_book
---

<objective>
Create test scaffolds for all DISC-01/02/03/04 behaviors before any production code exists.
Tests start in RED (failing due to missing hooks/endpoints) and turn GREEN after Plans 02-04 complete.

Purpose: Nyquist rule compliance — every automated verify step must reference a real test file.
Output: Three test files seeded with behavior contracts the executor can run.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-social-discovery-recommendations/15-RESEARCH.md
@frontend/src/hooks/__tests__/useNotifications.test.tsx
@frontend/src/components/social/__tests__/PendingRequestsWidget.test.tsx
@src/test/java/com/booktracker/social/SocialIntegrationTest.java
@frontend/src/lib/queryKeys.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FeedPage.test.tsx with MSW mocks for DISC-01/02/04</name>
  <files>frontend/src/pages/social/FeedPage.test.tsx</files>
  <read_first>
    - frontend/src/hooks/__tests__/useNotifications.test.tsx (MSW handler pattern, renderWithProviders helper)
    - frontend/src/components/social/__tests__/PendingRequestsWidget.test.tsx (MSW pattern for social hooks)
    - frontend/src/pages/social/FeedPage.tsx (component under test — imports, props, structure)
    - frontend/src/lib/queryKeys.ts (QUERY_KEYS.feed(), QUERY_KEYS.friendsReading())
    - frontend/src/types/api.types.ts (FeedItem shape)
  </read_first>
  <action>
Create `frontend/src/pages/social/FeedPage.test.tsx`. The test file must:

1. Import `http` and `HttpResponse` from `msw`, `server` from the existing MSW test setup (check useNotifications.test.tsx for the import path pattern — it will be something like `../../mocks/server` or `../../lib/mswServer`).

2. Define two MSW request handlers:
   - `http.get('/api/feed', ...)` returning a Page shape: `{ content: [], number: 0, size: 20, totalPages: 0, totalElements: 0 }`
   - `http.get('/api/feed/friends-reading', ...)` returning a Page shape: `{ content: [{ entryId: 'fr-1', userId: 'u-2', displayName: 'Alice', bookTitle: 'Dune', bookOlKey: 'OL123W', bookCoverId: null, bookAuthors: 'Frank Herbert' }], number: 0, size: 10, totalPages: 1, totalElements: 1 }`

3. Write three test cases (all will FAIL initially — Wave 0 RED phase):

   **Test A (DISC-01): "renders Friends are reading section heading"**
   - Render `<FeedPage />` with providers
   - Assert `screen.getByText('Friends are reading')` is present
   - Assert `screen.queryByText('No activity yet')` is NOT the only content (discovery content exists)

   **Test B (DISC-02): "book card in discovery section links to /books/:olKey"**
   - Render `<FeedPage />` with providers
   - Wait for `screen.findByText('Dune')` to appear
   - Assert a link element with `href` containing `/books/OL123W` exists in the document

   **Test C (DISC-04): "Friends finished recently section is present below discovery"**
   - Render `<FeedPage />` with providers
   - Assert `screen.getByText(/Friends finished recently/i)` or equivalent section heading is present

Use `renderWithProviders` from the same test utility used by `PendingRequestsWidget.test.tsx`. If the utility lives at a different path, locate it by reading the import in the existing test file.

Do NOT import `useFriendsReading` directly — test through the rendered component only.
  </action>
  <verify>
    <automated>cd /home/katlego/projects/booktracker/frontend && npm test -- --run src/pages/social/FeedPage.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>File exists, imports compile (no TS errors on imports that already exist), tests are collected by Vitest even if they fail at runtime due to missing hooks. Zero compilation errors; test count >= 3.</done>
  <acceptance_criteria>
    - `frontend/src/pages/social/FeedPage.test.tsx` exists
    - `npm test -- --run src/pages/social/FeedPage.test.tsx` collects 3 test cases (may FAIL at runtime — expected in Wave 0)
    - No TypeScript import errors for modules that already exist (msw, react-testing-library, queryKeys, FeedPage)
    - MSW handlers mock both `/api/feed` and `/api/feed/friends-reading`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Extend useSocial.test.ts for DISC-03 follow-count invalidation</name>
  <files>frontend/src/hooks/__tests__/useSocial.test.ts</files>
  <read_first>
    - frontend/src/hooks/__tests__/useNotifications.test.tsx (full file — hook test pattern with queryClient)
    - frontend/src/hooks/useSocial.ts (useFollowUser, useUnfollowUser, usePublicProfile signatures)
    - frontend/src/lib/queryKeys.ts (QUERY_KEYS.profile shape)
  </read_first>
  <action>
Check if `frontend/src/hooks/__tests__/useSocial.test.ts` already exists. If it does, append to it. If it does not exist, create it.

Add a test block for DISC-03: "follow mutation invalidates profile query so counts update immediately".

The test must:
1. Create a `QueryClient` with `defaultOptions: { queries: { retry: 0 } }`
2. Mock `POST /api/users/u-target/follow` to return `{ following: true }` with status 201
3. Mock `GET /api/users/u-target/profile` to return a minimal `PublicProfile` shape: `{ userId: 'u-target', displayName: 'Alice', followerCount: 0, followingCount: 0, isFollowing: false, booksReadThisYear: 0, readEntries: { content: [], number: 0, size: 20, totalPages: 0, totalElements: 0 } }`
4. Pre-populate the query cache with `queryClient.setQueryData(QUERY_KEYS.profile('u-target', 0), ...)` containing `followerCount: 5`
5. Call `useFollowUser('u-target')` via `renderHook` with the wrapped QueryClient
6. Call `mutate()` and wait for it to settle
7. Assert that `queryClient.getQueryState(QUERY_KEYS.profile('u-target'))?.isInvalidated` is true OR that a new query for the profile key was triggered (the query should no longer be fresh)

The test will FAIL until Plan 03 fixes the invalidation with `exact: false`. That is expected — this is the RED phase.
  </action>
  <verify>
    <automated>cd /home/katlego/projects/booktracker/frontend && npm test -- --run src/hooks/__tests__/useSocial.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>Test file exists with at least one test case targeting DISC-03; Vitest collects it; no TS compile errors on existing imports.</done>
  <acceptance_criteria>
    - `frontend/src/hooks/__tests__/useSocial.test.ts` exists (created or extended)
    - Test case named "follow mutation invalidates profile query" (or similar) is present
    - `npm test -- --run src/hooks/__tests__/useSocial.test.ts` collects the test (may fail at runtime — expected)
    - Test uses `QUERY_KEYS.profile('u-target')` (not a hard-coded string) to assert invalidation key
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add friends-reading integration test to SocialIntegrationTest.java</name>
  <files>src/test/java/com/booktracker/social/SocialIntegrationTest.java</files>
  <read_first>
    - src/test/java/com/booktracker/social/SocialIntegrationTest.java (full file — existing test structure, helper methods, seed patterns)
    - src/main/java/com/booktracker/social/FriendRequestRepository.java (findFeedForFriends as the template)
    - src/main/java/com/booktracker/shelf/UserBookEntity.java (if needed for field names)
  </read_first>
  <action>
Append a new test method `friendsReadingEndpointReturnsCurrentlyReadingBooks` to `SocialIntegrationTest.java`.

The test must:
1. Register two users (Alice and Bob) using the existing registration helper pattern already in the file
2. Create a CURRENTLY_READING `UserBookEntity` entry for Bob (seed directly via repository — same pattern as the existing feed tests)
3. Create an ACCEPTED `FriendRequestEntity` row between Alice and Bob (Alice is requester, Bob is recipient, status ACCEPTED) — seed via `FriendRequestRepository` or `EntityManager` following the existing pattern
4. Authenticate as Alice (get JWT via the existing login helper)
5. Call `GET /api/feed/friends-reading` with Alice's JWT
6. Assert:
   - Response status is 200
   - Response body has `content` array of length >= 1
   - The first item's `bookOlKey` matches the book seeded for Bob
   - `bookTitle` matches

Also add a negative test `friendsReadingDoesNotReturnNonFriendsBooks`:
1. Register Carol (no friend relationship with Alice)
2. Seed a CURRENTLY_READING book for Carol
3. Call `GET /api/feed/friends-reading` as Alice
4. Assert Carol's book does NOT appear in the content

These tests will fail with 404 until Plan 02 adds the endpoint. That is expected.
  </action>
  <verify>
    <automated>cd /home/katlego/projects/booktracker && ./mvnw test -pl . -Dtest=SocialIntegrationTest#friendsReadingEndpointReturnsCurrentlyReadingBooks,SocialIntegrationTest#friendsReadingDoesNotReturnNonFriendsBooks -q 2>&1 | tail -30</automated>
  </verify>
  <done>Two new test methods exist in SocialIntegrationTest.java; Maven compiles them; they are discovered by JUnit even if they fail at runtime (expected in Wave 0).</done>
  <acceptance_criteria>
    - `SocialIntegrationTest.java` contains method `friendsReadingEndpointReturnsCurrentlyReadingBooks`
    - `SocialIntegrationTest.java` contains method `friendsReadingDoesNotReturnNonFriendsBooks`
    - `./mvnw test-compile` completes without error on the test file
    - The negative test explicitly asserts Carol's book olKey is NOT in the response content list
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → application | Test files call the real application via HTTP or interact with the query client directly |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|-----------|----------|-----------|----------|-------------|------------|
| T-15-SC | Tampering | npm install (none in this plan) | low | accept | No new packages installed in Wave 1 |
</threat_model>

<verification>
All three test files exist and are collected by their respective test runners. Tests may fail at runtime (RED phase is expected). Zero compilation errors.
</verification>

<success_criteria>
- `frontend/src/pages/social/FeedPage.test.tsx` exists with 3 test cases
- `frontend/src/hooks/__tests__/useSocial.test.ts` exists with DISC-03 test
- `SocialIntegrationTest.java` has 2 new methods for friends-reading endpoint
- `./mvnw test-compile` passes
- `cd frontend && npm run build` or `tsc --noEmit` passes (no TS errors on test imports)
</success_criteria>

## Artifacts this phase produces

| Symbol | Type | File |
|--------|------|------|
| FeedPage test: "renders Friends are reading section heading" | Vitest test | `frontend/src/pages/social/FeedPage.test.tsx` |
| FeedPage test: "book card in discovery section links to /books/:olKey" | Vitest test | `frontend/src/pages/social/FeedPage.test.tsx` |
| FeedPage test: "Friends finished recently section is present" | Vitest test | `frontend/src/pages/social/FeedPage.test.tsx` |
| useSocial test: "follow mutation invalidates profile query" | Vitest test | `frontend/src/hooks/__tests__/useSocial.test.ts` |
| SocialIntegrationTest#friendsReadingEndpointReturnsCurrentlyReadingBooks | JUnit test | `src/test/java/com/booktracker/social/SocialIntegrationTest.java` |
| SocialIntegrationTest#friendsReadingDoesNotReturnNonFriendsBooks | JUnit test | `src/test/java/com/booktracker/social/SocialIntegrationTest.java` |

<output>
Create `.planning/phases/15-social-discovery-recommendations/15-01-SUMMARY.md` when done
</output>
